extends ../layout/layout

block content
    div.container-fluid
        form(method="post", action="/cat/update/"+cat.id)
            input(type='text', name='name', value=cat.name)
            input(type="text",name='age', value=cat.age)
            input(type="submit", value='Update')